# Deploy config. Pins Hugo so the build is deterministic and supports the
# newer template/config features used here (hugo.Data needs >= 0.156,
# the `locale` config key needs >= 0.158).
[build]
  publish = "public"
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.163.0"
